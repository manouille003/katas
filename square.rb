puts"Square every digit of a number and concatenate them :"

numbers = gets.chomp

# je veux dans un premier temps que le programme me demande d'entrer un nombre (un ou plusieurs chiffres)

 !numbers.is_a?(Integer)
    puts "please enter a real number"

# je veux ensuite qu'il décompose le nombre par les différent chiffre qui le composent
# each_number = numbers.to_s.chars.map(&:to_i)

# chaque chiffre va être mis au carré et afficher les uns à la suite des autres










